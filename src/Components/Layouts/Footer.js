import React from "react";
import { Layout } from "antd";
const { Footer } = Layout;

const FooterPages = () => {
  return <Footer className="site-layout-footer">FooterPages</Footer>;
};

export default FooterPages;
