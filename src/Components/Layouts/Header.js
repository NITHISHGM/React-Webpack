import React from "react";
import { Layout } from "antd";
const { Header } = Layout;

const HeaderPage = () => {
  return <Header className="site-layout-header">HeaderPage</Header>;
};

export default HeaderPage;
